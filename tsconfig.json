{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    // /* -------------------------------------------------------------------------- */
    // /*                                   regular                                  */
    // /* -------------------------------------------------------------------------- */
    "outDir": "dist",
    "baseUrl": "src",
    "paths": {
      "~/*": [
        "*" 
      ]
    }
  },
  "include": [
    "src"
  ],
  "exclude": [
    "node_modules"
  ]
}
